namespace: chlwndks3-namespace
resources:
  - nginx.yaml
  - mariadb.yaml
  - backend.yaml
#  - frontend.yaml
configMapGenerator:
  - name: nginx
    files:
      - configs/default.conf
  - name: init-db
    files:
      - configs/init.sql
